export enum Status {
  Active = "active",
  Inactive = "inactive",
}

export enum ActionType {
  Edit = "edit",
  Delete = "delete",
  Add = "add",
}

export enum ResponseType {
  Confirm = "confirm",
  Cancel = "cancel",
}
