export enum Status {
  Active = "active",
  Inactive = "inactive",
}

export enum ActionType {
  Edit = "edit",
  Delete = "delete",
}

export enum ResponseType {
  Confirm = "confirm",
  Cancel = "cancel",
}
